# 吃药么 · 离线运行依赖（无需联网）
pywebview>=5.0
# pywebview 的运行依赖（显式列出，确保一定被安装）
proxy_tools
bottle
typing_extensions
pythonnet>=3.0.3 ; sys_platform == 'win32'
pywin32>=305 ; sys_platform == 'win32'
# 离线拍照识别 + 离线语音
rapidocr-onnxruntime>=1.3.0
pyttsx3>=2.90
# 运行模式：托盘常驻（v3.1 新增）
pystray>=0.19
Pillow>=9.0
# 需求4：药盒识别药名模糊匹配（v3.1 新增）
rapidfuzz>=3.0
# 需求3/4：在线兜底查询 openFDA（v3.1 新增）
requests>=2.28
# 可选：仅“联网识别”功能需要（离线无需）： pip install anthropic
